#include <stdio.h>

int main() 
{
    int a;
    int b;
    int c=0;
    printf("enter the value = ");
    scanf("%d",&a);
    for(b=1;b<=a;b++)
    {
        c=c+b;
        printf("the values %d\n",b);
    }    
     printf(" sum=%d\n",c);
     return 0;
}
